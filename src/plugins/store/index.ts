import { createStore } from "vuex";

interface ApplicationStore { } // eslint-disable-line

export default createStore<ApplicationStore>({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
